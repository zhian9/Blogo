# Blogo — Prometheus 监控配置
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: blogo-api
    metrics_path: /metrics
    static_configs:
      - targets: ['blogo-api:9101']
